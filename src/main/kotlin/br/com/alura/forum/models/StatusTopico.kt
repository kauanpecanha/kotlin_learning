package br.com.alura.forum.models

// entidade para representar os status de tópicos dentro da plataforma, no formtato de enum para padronização
enum class StatusTopico {
    // enumerando os atributos possíveis de statustopico
    NAO_RESPONDIDO,
    NAO_SOLUCIONADO,
    SOLUCIONADO,
    FECHADO
}
